# 03 Modules

本 lesson 对应原始 `commonjs` 目录，把模块机制整理成 ESM 主线和 CommonJS 对照。

重点：

- ESM `import` / `export`
- CommonJS `require` / `module.exports`
- 模块加载副作用
- 模块缓存

运行：

```bash
pnpm lesson:03
```

CommonJS 对照文件在 `src/commonjs-demo.cjs`。
