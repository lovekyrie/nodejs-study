# 04 Events and Async I/O

本 lesson 对应原始 `3.node.js` 和 `4.nonblocking` 目录，整理为可停止、可测试的 EventEmitter 和异步 I/O 示例。

重点：

- `EventEmitter`
- `setInterval` / `clearInterval`
- 同步 I/O 与异步 I/O 对比
- `async` / `await`

运行：

```bash
pnpm lesson:04
```
